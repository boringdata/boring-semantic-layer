name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:
  schedule:
    # Weekly ibis version matrix (see the ibis-matrix job).
    - cron: "0 6 * * 1"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name != 'release' }}

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run Ruff lint
        run: uv run ruff check .

      - name: Check formatting
        run: uv run ruff format --check .

  # Run the test suite with and without the xorq extra. The no-xorq leg
  # verifies xorq is truly optional (its tests skip, not error).
  test:
    name: Test (${{ matrix.name }}, py${{ matrix.python }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: full
            python: "3.11"
            sync: uv sync --all-extras
          - name: full
            python: "3.12"
            sync: uv sync --all-extras
          - name: full
            python: "3.13"
            sync: uv sync --all-extras
          - name: no-xorq
            python: "3.12"
            sync: uv sync --all-extras --no-extra xorq --no-extra examples
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies (${{ matrix.name }})
        run: ${{ matrix.sync }}

      - name: Verify import works
        run: uv run python -c "import boring_semantic_layer; print('import OK')"

      - name: Run tests
        run: uv run pytest -n auto

      - name: Run no-xorq-compatible examples
        if: matrix.name == 'no-xorq'
        run: make examples-core

  # Weekly: run the suite against every supported ibis version so an
  # upstream release can't silently break us between our own pushes.
  ibis-matrix:
    name: Test (ibis version matrix)
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Run tests across ibis versions
        run: make test IBIS_VERSION=all

  # Build examples, docs, and skills (require the full env incl. Node).
  build:
    name: Build (examples + docs + skills)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: docs/web/package-lock.json

      - name: Install dependencies
        run: |
          uv sync --all-extras
          cd docs/web && npm ci

      - name: Run examples
        run: make examples

      - name: Build docs
        run: make docs-build

      - name: Check skills are up to date
        run: make skills-check

      - name: Upload docs artifact
        if: github.event_name == 'release'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/web/dist

  # Deploy docs to GitHub Pages (only on release - keep docs in sync with PyPI package)
  deploy-docs:
    name: Deploy Docs
    if: github.event_name == 'release'
    needs: [lint, test, build]
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  # Release to PyPI (only on GitHub release)
  release-pypi:
    name: Release to PyPI
    if: github.event_name == 'release'
    needs: [lint, test, build]
    runs-on: ubuntu-latest
    environment:
      name: release
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --all-extras

      - name: Build package
        run: uv build

      - name: Test wheel installation in clean environment
        run: |
          python -m venv test_env
          test_env/bin/pip install dist/*.whl
          test_env/bin/python -c "import boring_semantic_layer; print('✓ Successfully imported boring-semantic-layer')"

      - name: Run tests with installed package
        run: uv run pytest

      - name: Publish to PyPI
        run: uv publish --trusted-publishing always

      - name: Cleanup temporary artifacts
        if: always()
        run: |
          rm -rf test_env dist
